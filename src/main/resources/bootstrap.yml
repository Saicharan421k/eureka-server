spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      retry:
        initial-interval: 1500
        multiplier: 1.5
        max-interval: 4000
        max-attempts: 10